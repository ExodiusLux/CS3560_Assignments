package Assignment2;

public class Payroll {
	public static void processPayments(Worker worker) { //uses worker class as param to print worker name
		System.out.println("Payment processed for worker " + worker.getName());
	}
}	
